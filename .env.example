# Required for OCR with Gemini Vision.
GEMINI_API_KEY=
GEMINI_MODEL=gemini-2.5-flash

# Required for GPT-4o-mini explanation and AI review.
OPENAI_API_KEY=
OPENAI_MODEL=gpt-4o-mini

# Optional: only needed when deploying from Railway CLI/CI.
RAILWAY_TOKEN=
